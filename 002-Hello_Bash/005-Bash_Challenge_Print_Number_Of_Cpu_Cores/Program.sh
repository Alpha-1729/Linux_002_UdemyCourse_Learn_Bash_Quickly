#!/bin/bash
# Bash Challenge Print Number Of Cpu Cores

: '
>>>>
>>>>
>>>>
>>>>
'

# Print the number of CPU cores.
# The -n option for echo prevents a newline, so the output is on the same line as the CPU count.
echo -n "Number of CPU(s): ";nproc

echo "Hello World"
